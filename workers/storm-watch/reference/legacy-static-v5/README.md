Static multi-page snapshot found alongside the dev source (filename indicated
"_5", likely an earlier prototype iteration before the app moved to the
Cloudflare Worker + D1 architecture in `../../`). Kept for reference only —
not part of the deployed app. `index.html`/`birds.html`/`rules.html`/`storms.html`
are standalone pages with their own nav, not wired to the worker API.
